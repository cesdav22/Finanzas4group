import { SeparadorMilesPipe } from './separador-miles-pipe.pipe';

describe('SeparadorMilesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SeparadorMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
